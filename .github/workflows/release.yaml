name: Release
on:
  push:
    tags:
      - v*.*.*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.ASS_HERO_CHI}}
          heroku_app_name: "comies-beta"
          heroku_email: "eakeur@icloud.com"
